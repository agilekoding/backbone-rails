.row
  .span12
    .page-header
      %h1= t(".title")
      %ul.tabs
        %li
          = link_to t("links.new", :resource => <%= classify_model_name %>.model_name.human), "#/new"

.row
  .span12
    %table#<%= plural_name %>_table.table.table-striped
      %thead
        %tr
<% attributes.each do |attribute| -%>
          %th= <%= human_attribute_translate(attribute.name) %>
<% end -%>
          %th.actions
            = link_to "", "#", :class => "icon-refresh", :id => "refresh_<%= plural_name %>", :title => t("links.refresh"), :alt => t("links.refresh")

      %tbody

#pagination-container

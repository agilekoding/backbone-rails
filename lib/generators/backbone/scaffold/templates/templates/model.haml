<% attributes.each do |attribute| -%>
%td ${<%= attribute.name %>}
<% end -%>

%td.links
  = link_to "Show", "#/${id}".html_safe, :class => "show"
  = link_to "Edit", "#/${id}/edit".html_safe, :class => "edit"
  = link_to "Destroy", "#/${id}/destroy".html_safe, :class => "destroy"
